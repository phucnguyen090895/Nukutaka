@using Nukutaka.Models;
@model PRODUCT
@{
    ViewBag.Title = "Detail";
    Layout = "~/Views/Shared/_LayoutLeft.cshtml";
}
<div class="product-details">
    <!--product-details-->
    <div class="col-sm-5">
        <div class="view-product">
            <img src="~/images/products/@Model.IMAGEURL" alt="@Model.NAME" height="381" width="266"/>
            <h3>ZOOM</h3>
        </div>
    </div>
    <div class="col-sm-7">
        <div class="product-information">
            <!--/product-information-->
        @if (Model.INSTOCK == 0)
        {
            <img src="~/images/product-details/Out-of-stock3.jpg" class="newarrival" alt="" />
        }
            <h2>@Model.NAME</h2>
            <p>@Model.CODE</p>
            <img src="~/images/product-details/rating.png" alt="" />
        @using (Html.BeginForm("AddCart", "Cart"))
        {    
            <span>
                <span>@String.Format("{0:0,0}",@Model.PRICE)</span>
                    <label>Số lượng:</label>
                    <input type="number" id="iQuantity" name="iQuantity" min="1" value="1"/>
                    <input type="hidden" id="code"name="code" value="@Model.CODE" />
                    <input type="hidden" id="urlCurrent" name="urlCurrent" value="@Request.Url.ToString()" />
                @if (Model.INSTOCK == 0)
                {
                    <button type="button" class="btn btn-fefault cart">
                        Hết hàng
                    </button>
                }
                else
                {
                    <button type="submit" class="btn btn-fefault cart">
                        <i class="fa fa-shopping-cart"></i>
                        Thêm vào giỏ
                    </button>
                }                           
            </span>
        }
            <p><b>Thời trang:</b> @ViewBag.CategoryName</p>
            <p><b>Loại:</b> @ViewBag.KindName</p>
            <p><b>Thương hiệu:</b> @ViewBag.BrandName</p>
            <a href=""><img src="~/images/product-details/share.png" class="share img-responsive" alt="" /></a>
        </div><!--/product-information-->
    </div>
</div>

